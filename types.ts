// Compatibility entry point for screens and data importing @/types.
export type {
  Movie, MovieVideo, VideoSource, FeatureKey, SwipeDirection,
  TasteVector, Match, Member, Session,
} from './src/types';
export { FEATURE_PREFIX } from './src/types';
